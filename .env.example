# Docker Compose configuration
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=taskdb
JWT_SECRET=change-this-to-a-long-random-secret-for-local-dev

# Ollama runs on the Docker host by default. Change this if Ollama is remote.
LLM_SERVER=http://host.docker.internal:11434
OLLAMA_MODEL=gemma4:e4b
